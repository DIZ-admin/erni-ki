---
language: ru
translation_status: original
doc_version: '2025.11'
last_updated: '2025-11-30'
---

# АУДИТ РАСХОЖДЕНИЙ ДОКУМЕНТАЦИИ ERNI-KI

**Дата аудита:** 2025-11-30 **Статус:** КРИТИЧНЫЕ РАСХОЖДЕНИЯ ОБНАРУЖЕНЫ
**Приоритет:** ВЫСОКИЙ - требует немедленной синхронизации

---

## РЕЗЮМЕ

Обнаружены критические расхождения между русской/английской документацией и
фактической кодовой базой проекта ERNI-KI. Основные проблемы:

- **Структурные:** Отсутствие русской директории локализации (`docs/ru/`)
- **Версионирование:** 8+ компонентов с устаревшими версиями в документации
- **Инвентаризация:** Противоречивые данные о количестве сервисов (30/32/34)
- **Документирование:** 6+ сервисов без документации (Uptime Kuma, MCP Server,
  Docling)
- **Конфигурация:** Отсутствует руководство по secrets, env-файлам, GPU
  оптимизации
- **API:** Нет документации для webhook-receiver, MCP Server endpoints

---

## 1. КРИТИЧЕСКИЕ ПРОБЛЕМЫ СТРУКТУРЫ

### 1.1 Отсутствие директории русской локализации

**Текущее состояние:**

```
docs/
 en/ Существует (16 директорий)
 de/ Существует (16 директорий)
 ru/ НЕ СУЩЕСТВУЕТ
 [root files] Частичная русская документация в корне
```

**Конфигурация в mkdocs.yml:**

```yaml
language: 'ru' # Строка 21 - русский язык по умолчанию
plugins:
 - i18n:
 default_language: ru # Строки 87-88
 languages:
 ru: Русский
 en: English
 de: Deutsch
 docs_structure: folder # Ожидает структуру docs/{lang}/
```

**Проблемы:**

- MkDocs ищет русскую документацию в `docs/ru/`, но её нет
- Языковой переключатель не работает корректно для русского
- Поисковая индексация нарушена
- SEO оптимизация снижена

**Решение:** Либо создать `docs/ru/` и мигрировать русскую документацию, либо
изменить конфигурацию i18n на `docs_structure: suffix` и переименовать файлы на
`*.ru.md`.

**Приоритет:** КРИТИЧНЫЙ (блокирует правильную локализацию)

---

## 2. НЕСООТВЕТСТВИЯ ВЕРСИЙ КОМПОНЕНТОВ

### Таблица версий

| Компонент        | Версия в docs | Фактическая версия | Расхождение | Статус   |
| ---------------- | ------------- | ------------------ | ----------- | -------- |
| **OpenWebUI**    | v0.6.36       | v0.6.40            | -4 минор    | Отстает  |
| **Ollama**       | 0.12.11       | 0.13.0             | -1 минор    | Отстает  |
| **Prometheus**   | v3.0.0        | v3.7.3             | -7 патч     | Отстает  |
| **Grafana**      | v11.3.0       | v12.3.0            | -1 МАЖОР    | Критично |
| **Loki**         | v3.0.0        | v3.6.2             | -6 минор    | Отстает  |
| **Fluent Bit**   | v3.1.0        | v4.2.0             | -1 МАЖОР    | Критично |
| **Alertmanager** | v0.27.0       | v0.29.0            | -2 минор    | Отстает  |
| **Nginx**        | 1.28.0        | 1.29.3             | -1 минор    | Отстает  |
| **Backrest**     | v1.9.2        | v1.10.1            | -1 минор    | Отстает  |

### Детали по каждому компоненту

#### OpenWebUI

**Файлы с устаревшей информацией:**

- `docs/index.md:18`
- `docs/overview.md:18, 76`
- `docs/architecture/services-overview.md:19`

**Расхождение:** 4 минорные версии (v0.6.36 → v0.6.40)

**Фактическое значение из compose.yml:502:**

```yaml
ghcr.io/open-webui/open-webui:v0.6.40
```

#### Grafana (КРИТИЧНЫЕ ИЗМЕНЕНИЯ)

**Файлы с устаревшей информацией:**

- `docs/index.md:20`
- `docs/overview.md:63`
- `docs/architecture/architecture.md:17`

**Расхождение:** МАЖОРНАЯ ВЕРСИЯ (v11.3.0 → v12.3.0)

**Изменения в v12.x:**

- Новый интерфейс с улучшенной производительностью
- Изменённые API endpoints
- Несовместимость плагинов (требуется обновление)
- Новая система управления сигналами

**Фактическое значение из compose.yml:672:**

```yaml
grafana/grafana:12.3.0
```

**Воздействие:** Пользователи могут столкнуться с несовместимыми плагинами,
неработающими dashboard, API ошибками.

#### Fluent Bit (КРИТИЧНЫЕ ИЗМЕНЕНИЯ)

**Файлы с устаревшей информацией:**

- `docs/index.md:20`
- `docs/overview.md:63`
- `docs/architecture/architecture.md:17`

**Расхождение:** МАЖОРНАЯ ВЕРСИЯ (v3.1.0 → v4.2.0)

**Комментарий в compose.yml:1153:**

```
# major update 3.x -> 4.2.0
```

**Изменения в v4.x:**

- Полная переработка конфигурационного формата
- Новые input/output плагины
- Улучшенная обработка метрик
- Breaking changes в YAML конфигурации

**Фактическое значение из compose.yml:1153:**

```yaml
fluent/fluent-bit:4.2.0
```

**Воздействие:** Логирование может разломаться при обновлении, требуется
миграция конфигурации.

---

## 3. НЕСООТВЕТСТВИЕ КОЛИЧЕСТВА СЕРВИСОВ

### Проблема: Три разных числа в документации

| Документ                                    | Количество | Статус  |
| ------------------------------------------- | ---------- | ------- |
| `docs/index.md:15`                          | **34/34**  | НЕВЕРНО |
| `docs/overview.md:15, 62`                   | **34/34**  | НЕВЕРНО |
| `docs/architecture/architecture.md:18`      | **32/32**  | ВЕРНО   |
| `docs/architecture/services-overview.md:11` | **30/30**  | НЕВЕРНО |
| **Фактическое значение**                    | **32**     | ВЕРНО   |

### Полный список сервисов (32 шт.)

```
Управление системой (3):
1. watchtower - автоматическое обновление контейнеров
2. db - PostgreSQL
3. redis - кеш и очередь сообщений

Основные сервисы (6):
4. litellm - API для LLM
5. auth - аутентификация (Go)
6. cloudflared - туннель Cloudflare
7. edgetts - текст в речь
8. tika - извлечение текста из документов
9. mcposerver - MCP сервер (7 инструментов)

Основное приложение (3):
10. searxng - поисковая система
11. ollama - локальные LLM модели
12. nginx - обратный прокси
13. openwebui - веб-интерфейс
14. docling - обработка документов

Backup и восстановление (1):
15. backrest - backup система

Мониторинг и логирование (12):
16. prometheus - сбор метрик
17. grafana - визуализация
18. uptime-kuma - мониторинг uptime НЕ ДОКУМЕНТИРОВАН
19. loki - агрегация логов
20. alertmanager - управление алертами
21. node-exporter - метрики хоста
22. postgres-exporter - метрики БД
23. postgres-exporter-proxy - IPv4→IPv6 прокси НЕ ДОКУМЕНТИРОВАН
24. nvidia-exporter - метрики GPU
25. blackbox-exporter - проверка endpoints
26. redis-exporter - метрики Redis
27. ollama-exporter - метрики Ollama
28. nginx-exporter - метрики Nginx
29. cadvisor - метрики контейнеров
30. fluent-bit - сбор логов
31. rag-exporter - метрики RAG
32. webhook-receiver - получение алертов
```

**Решение:** Обновить все документы на число **32**, с указанием критичных
сервисов, опциональных, и монитор-only.

---

## 4. НЕДОКУМЕНТИРОВАННЫЕ КОМПОНЕНТЫ

### 4.1 Uptime Kuma

**Конфигурация в compose.yml (строки 710-737):**

```yaml
uptime-kuma:
  image: louislam/uptime-kuma:2.0.2
  container_name: erni-ki-uptime-kuma
  ports:
    - '127.0.0.1:3001:3001'
  volumes:
    - uptime-kuma-data:/app/data
  environment:
    - PUID=1000
    - PGID=1000
    - UMASK=0022
    - TZ=${TZ}
```

**Проблемы:**

- Не упомянут в `docs/index.md`
- Не упомянут в `docs/overview.md`
- Не упомянут в `docs/architecture/architecture.md`
- Не документирована интеграция с Prometheus/Grafana
- Нет примеров конфигурации мониторов

**Рекомендация:** Добавить раздел "Uptime Monitoring" с описанием возможностей и
примерами.

### 4.2 Postgres-exporter-proxy (socat)

**Конфигурация в compose.yml (строки 911-936):**

```yaml
postgres-exporter-proxy:
  image: alpine:latest
  container_name: erni-ki-postgres-exporter-proxy
  command: socat -4 TCP-LISTEN:9188,reuseaddr,fork TCP:postgres-exporter:9187
  ports:
    - '127.0.0.1:9188:9188'
  depends_on:
  postgres-exporter:
  condition: service_healthy
  mem_limit: 64m
  cpus: '0.1'
  restart: unless-stopped
```

**Проблемы:**

- Полностью отсутствует в документации
- Назначение не объяснено (IPv4→IPv6 проксирование)
- Нет информации о troubleshooting
- Порт 9188 не упомянут в документации мониторинга (документация указывает 9187)

**Рекомендация:** Добавить раздел о networking issues и объяснить использование
socat для IPv6 compatibility.

### 4.3 Docling (недостаточно документирован)

**Конфигурация в compose.yml (строки 542-580):**

```yaml
docling:
  image: ${DOCLING_IMAGE:-ghcr.io/ds4sd/docling:0.48.0}
  container_name: erni-ki-docling
  environment:
    - CUDA_VISIBLE_DEVICES=0
    - MODEL_PRECISION=float32
  mem_limit: 12g
  mem_reservation: 8g
  cpus: '8.0'
```

**Проблемы:**

- Базовое описание есть (`docs/getting-started/installation.md:102-109`)
- Нет документации поддерживаемых языков (EN, DE, FR, IT)
- Нет API reference для document processing endpoints
- Нет примеров использования с OpenWebUI
- Нет информации о производительности и требованиях GPU
- Нет troubleshooting guide для ошибок OCR

**Рекомендация:** Создать раздел "Document Processing with Docling" с полным API
reference.

### 4.4 MCP Server (недостаточно документирован)

**Конфигурация в compose.yml (строки 347-379):**

```yaml
mcposerver:
  image: ghcr.io/open-webui/mcpo:git-91e8f94
  container_name: erni-ki-mcpo-server
  ports:
    - '127.0.0.1:8000:8000'
  environment:
    - OLLAMA_BASE_URL=http://ollama:11434
    - DATABASE_URL=postgresql://...
```

**7 встроенных инструментов упомянуты только в `docs/overview.md:80`:**

1. Time - получение текущего времени
2. Context7 Docs - работа с документацией
3. PostgreSQL - запросы к БД
4. Filesystem - работа с файлами
5. Memory - управление памятью
6. SearXNG Web Search - веб-поиск
7. Desktop Commander - выполнение команд

**Проблемы:**

- Нет подробного описания каждого инструмента
- Нет API reference
- Нет примеров использования в prompts
- Нет информации о том, как добавить новые tools
- Нет troubleshooting guide

**Рекомендация:** Создать раздел "MCP Server Tools" с документацией каждого
инструмента.

---

## 5. НЕДОСТАЮЩАЯ ДОКУМЕНТАЦИЯ ОКРУЖЕНИЯ

### 5.1 Env-файлы (не указано, какие нужны)

**Документация указывает:** `docs/getting-started/installation.md:150-161`

```
Копирование примеров конфигураций (оптимизированная структура)
```

**Проблема:** Нет полного списка требуемых env-файлов.

**Фактический список требуемых env-файлов из compose.yml:**

```
Обязательные:
- env/db.env (PostgreSQL пароль, параметры)
- env/redis.env (Redis пароль, конфигурация)
- env/watchtower.env (параметры автообновления)
- env/auth.env (WEBUI_SECRET_KEY для JWT)
- env/openwebui.env (API ключи, конфигурация)

Для LLM:
- env/ollama.env (параметры моделей, GPU)
- env/litellm.env (API ключи: OpenAI, Claude, PaLM)

Для интеграций:
- env/searxng.env (поисковые API)
- env/edgetts.env (текст в речь параметры)
- env/cloudflared.env (токен туннеля)

Для обработки документов:
- env/tika.env (Tika сервер параметры)
- env/docling.env (OCR параметры, GPU)
- env/mcposerver.env (MCP сервер конфигурация)

Для резервного копирования:
- env/backrest.env (расписание, хранилище)

Для мониторинга:
- env/grafana.env (пароль админа, плагины)
- env/prometheus.env (retention period, scrape interval)
- env/loki.env (хранение логов)
- env/alertmanager.env (SMTP для уведомлений)

Опциональные:
- env/uptime-kuma.env (конфигурация мониторинга)
- env/cadvisor.env (параметры сбора метрик)
```

**Решение:** Создать раздел "Environment Configuration" с полным списком всех
env-файлов, их назначением, обязательными переменными и значениями по умолчанию.

### 5.2 Docker Secrets (полностью отсутствует документация)

**Конфигурация в compose.yml (строки 1299-1364):**

```yaml
secrets:
 redis_password:
 file: ./secrets/redis_password.txt
 postgres_password:
 file: ./secrets/postgres_password.txt
 openai_api_key:
 file: ./secrets/openai_api_key.txt
 # ... и ещё 20+ секретов
```

**Проблема:**

- Документация **полностью отсутствует** для secrets
- Нет инструкций по их созданию
- Нет примеров содержимого файлов
- Нет информации о permissions (должны быть 600)
- Нет информации о ротации секретов

**Критичные секреты:**

1. `redis_password` - доступ к Redis
2. `postgres_password` - доступ к БД
3. `openai_api_key` - OpenAI API
4. `anthropic_api_key` - Claude API
5. `google_api_key` - Google API
6. `openwebui_secret_key` - JWT секрет
7. `litellm_api_key` - LiteLLM сервис
8. `litellm_master_key` - LiteLLM мастер-ключ
9. `litellm_salt_key` - LiteLLM соль
10. `context7_api_key` - Context7 интеграция

**Решение:** Создать раздел "Secrets Management" с пошаговым руководством:

```bash
# Создание директории с правильными permissions
mkdir -p secrets
chmod 700 secrets

# Создание каждого secret файла
echo "your-redis-password" > secrets/redis_password.txt
chmod 600 secrets/redis_password.txt
```

---

## 6. ПРОБЕЛЫ В ДОКУМЕНТАЦИИ API

### 6.1 Webhook Receiver API (критично для мониторинга)

**Сервис в compose.yml (строки 1227-1266):**

```yaml
webhook-receiver:
  image: ${WEBHOOK_RECEIVER_IMAGE:-erni-ki-webhook-receiver:dev}
  container_name: erni-ki-webhook-receiver
  ports:
    - '127.0.0.1:9095:9093'
  environment:
    - ALERTMANAGER_WEBHOOK_SECRET=${ALERTMANAGER_WEBHOOK_SECRET}
```

**Код сервиса:**

- `conf/webhook-receiver/webhook_handler.py` (365 строк)
- `conf/webhook-receiver/webhook-receiver.py` (408 строк)

**Endpoints в коде:**

- `POST /webhook` - общие алерты
- `POST /webhook/critical` - критичные алерты
- `POST /webhook/warning` - предупреждения
- `POST /webhook/gpu` - GPU алерты
- `POST /webhook/ai` - AI сервис алерты
- `POST /webhook/database` - БД алерты
- `GET /health` - health check
- `GET /alerts` - список последних алертов

**Проблемы:**

- Нет документации payload format
- Нет примеров запросов
- Нет информации о аутентификации (X-Signature header)
- Нет информации о HMAC-SHA256 подписи
- Нет примеров интеграции с Alertmanager

**Решение:** Создать раздел "Webhook Receiver API Reference" с:

```yaml
POST /webhook
Content-Type: application/json
X-Signature: hmac-sha256-signature

{
 "alerts": [
 {
 "status": "firing|resolved",
 "labels": {
 "alertname": "DiskFull",
 "severity": "critical|warning|info",
 "service": "ollama|openwebui|searxng"
 },
 "annotations": {
 "summary": "Disk is full",
 "description": "Root partition 98% full"
 }
 }
 ],
 "groupLabels": {}
}
```

### 6.2 MCP Server Endpoints (недокументировано)

**Конфигурация в compose.yml:**

```yaml
mcposerver:
  ports:
    - '127.0.0.1:8000:8000'
```

**Проблемы:**

- Нет документации REST endpoints
- Нет информации о инструментах и их использовании
- Нет примеров запросов
- Нет информации о расширении с новыми tools

**Решение:** Создать раздел "MCP Server API" с документацией для каждого
инструмента.

---

## 7. ПРОБЛЕМЫ С ПОРТАМИ И HEALTH CHECKS

### 7.1 Несоответствие портов в документации

**Документация указывает (docs/getting-started/installation.md:256-274):**

```
PostgreSQL Exporter: localhost:9187/metrics
```

**Фактические порты в compose.yml:**

- postgres-exporter: строка 857 → порт `127.0.0.1:9187:9187` ВЕРНО
- postgres-exporter-proxy: строка 918 → порт `127.0.0.1:9188:9188` IPv4→IPv6
  прокси

**Проблема:**

- Документация не объясняет, почему нужны оба экспортера
- Нет информации о том, когда использовать какой порт
- Нет troubleshooting для ошибок подключения

### 7.2 Nginx healthcheck

**Конфигурация в compose.yml (строка 459):**

```yaml
healthcheck:
  test: ['CMD-SHELL', '/etc/nginx/healthcheck.sh']
```

**Скрипт монтируется из:** `conf/nginx/healthcheck.sh` (строка 477)

**Проблема:**

- Скрипт не документирован
- Нет информации о том, что он проверяет
- Нет troubleshooting для failed checks

---

## 8. НЕДОКУМЕНТИРОВАННАЯ СТРАТЕГИЯ ЛОГИРОВАНИЯ

### 8.1 4-tier Logging Strategy (полностью отсутствует в документации)

**Конфигурация в compose.yml (строки 1-50):**

**Tier 1 (Critical Services) - Maximum Reliability:**

```yaml
x-critical-logging: &critical-logging
  driver: 'json-file'
  options:
  max-size: '10m'
  max-file: '5'
  labels: 'service,version,environment,level=critical'
  tag: 'critical.{{.Name}}'
```

Сервисы: OpenWebUI, Ollama, PostgreSQL, Nginx

**Tier 2 (Important Services) - Standard Logging:**

```yaml
x-important-logging: &important-logging
  driver: 'json-file'
  options:
  max-size: '10m'
  max-file: '5'
  labels: 'service,version,environment,level=important'
```

Сервисы: SearXNG, Redis, Backrest, Auth, Cloudflared

**Tier 3 (Auxiliary Services) - Basic Logging:**

```yaml
x-auxiliary-logging: &auxiliary-logging
  driver: 'json-file'
  options:
  max-size: '10m'
  max-file: '5'
  labels: 'service,version,level=auxiliary'
```

Сервисы: Docling, EdgeTTS, Tika, MCP Server

**Tier 4 (Monitoring Services) - Minimal Logging:**

```yaml
x-monitoring-logging: &monitoring-logging
  driver: 'json-file'
  options:
  max-size: '10m'
  max-file: '5'
  labels: 'service,version,level=monitoring'
```

Сервисы: Prometheus, Grafana, Exporters, Alertmanager

**Проблемы:**

- Стратегия полностью отсутствует в документации
- Нет объяснения, почему каждый tier используется
- Нет информации о том, где собираются логи
- Нет информации о ротации логов
- Нет рекомендаций по изменению в production

**Решение:** Добавить раздел "Logging Architecture" с объяснением каждого tier и
рекомендациями по кастомизации.

### 8.2 Loki X-Scope-OrgID header

**Документация указывает (docs/getting-started/installation.md:259):**

```
используйте заголовок `X-Scope-OrgID: erni-ki`
```

**Проблемы:**

- Нет объяснения, что это за заголовок
- Нет информации о его назначении
- Нет примеров curl запросов с этим заголовком
- Нет информации о использовании через Grafana UI

---

## 9. НЕДОКУМЕНТИРОВАННЫЕ ОПТИМИЗАЦИИ

### 9.1 GPU Memory Management (OLLAMA_MAX_VRAM)

**Комментарий в compose.yml (строка 429-430):**

```
# GPU memory limits optimized for RTX 5000 (16GB VRAM)
# Ollama gets GPU priority (VRAM management via OLLAMA_MAX_VRAM=0.75)
```

**Конфигурация:**

- Ollama: `mem_limit: 24g`, VRAM allocation: 75%
- OpenWebUI: `mem_limit: 8g`, VRAM allocation: 20%
- Docling: `mem_limit: 12g`, VRAM allocation: 50%

**Проблемы:**

- Эти оптимизации вообще не документированы
- Нет информации о том, как адаптировать для других GPU
- Нет информации о том, что произойдет при нехватке памяти
- Нет примеров для других GPU моделей:
- RTX 3090 (24GB)
- RTX 4090 (24GB)
- A100 (40GB)
- H100 (80GB)

**Решение:** Создать раздел "GPU Optimization Guide" с рекомендациями для разных
моделей видеокарт.

### 9.2 OOM Score Adjustment (oom_score_adj)

**Конфигурация в compose.yml:**

```yaml
Ollama: oom_score_adj: -900 # Максимальная защита
OpenWebUI: oom_score_adj: -600
Docling: oom_score_adj: -500
LiteLLM: oom_score_adj: -300
webhook-receiver: oom_score_adj: 250 # Низкий приоритет (убить первым)
```

**Проблемы:**

- Стратегия не объяснена
- Нет информации о том, как работает OOM killer
- Нет рекомендаций по изменению в production
- Нет информации о том, какой сервис будет убит при недостатке памяти

**Решение:** Добавить раздел "Memory Management" с объяснением OOM killer
priority.

---

## 10. УСТАНОВКА И МИГРАЦИЯ

### 10.1 Несуществующие скрипты

**Документация ссылается на следующие скрипты, которые требуют проверки:**

| Скрипт                                            | Документация (строка) | Существует ли? |
| ------------------------------------------------- | --------------------- | -------------- |
| `./scripts/setup/setup.sh`                        | installation.md:131   | ?              |
| `./scripts/setup/quick-start.sh`                  | installation.md:134   | ?              |
| `./scripts/maintenance/health-check.sh`           | installation.md:141   | ?              |
| `./scripts/setup/gpu-setup.sh`                    | installation.md:216   | ?              |
| `./scripts/setup/deploy-monitoring-system.sh`     | installation.md:239   | ?              |
| `./scripts/backup/check-local-backup.sh`          | installation.md:425   | ?              |
| `./scripts/troubleshooting/automated-recovery.sh` | installation.md:490   | ?              |

**Решение:** Провести аудит всех ссылок на скрипты и либо удалить
несуществующие, либо создать их.

### 10.2 Watchtower selective updates (не документирована стратегия)

**Конфигурация в compose.yml:**

**С включенным автообновлением (enable: true):**

```
auth, cloudflared, edgetts, tika, mcposerver, searxng,
openwebui, docling, backrest, prometheus, grafana, uptime-kuma,
loki, alertmanager, node-exporter, postgres-exporter,
nvidia-exporter, blackbox-exporter, redis-exporter, ollama-exporter,
nginx-exporter, cadvisor, fluent-bit, rag-exporter, webhook-receiver
```

**С отключенным автообновлением (enable: false, monitor-only: true):**

```
watchtower, db (PostgreSQL), ollama, nginx
```

**Проблемы:**

- Политика автообновлений не документирована
- Нет объяснения, почему db и ollama не обновляются автоматически
- Нет информации о ручном обновлении критичных сервисов
- Нет информации о testing обновлений перед production

**Решение:** Добавить раздел "Update Strategy" с объяснением политики.

---

## 11. БЕЗОПАСНОСТЬ И COMPLIANCE

### 11.1 Контейнеры с root-правами (не документирована security policy)

**Контейнеры с user: "0" или user: "0:0":**

1. **watchtower** (строка 75): `user: "0:0"`

- Комментарий: "Run as root to guarantee access to docker.sock"
- Причина: Требует доступа к Docker API

2. **grafana** (строка 674): `user: "0"`

- Нет объяснения

**Проблемы:**

- Нет security policy документа
- Нет объяснения risks
- Нет плана миграции на non-root containers
- Нет best practices для production deployment

**Решение:** Создать раздел "Security Policy" с объяснением risks и
recommendations.

### 11.2 Secrets rotation (не документирована процедура)

**23 секретных файла в ./secrets/**

**Проблемы:**

- Нет документации по ротации секретов
- Нет информации о частоте ротации
- Нет процедуры ротации без downtime
- Нет backup strategy для secrets

**Решение:** Создать раздел "Secrets Rotation" с пошаговым руководством.

---

## 12. ПРИОРИТИЗИРОВАННЫЕ РЕКОМЕНДАЦИИ

### КРИТИЧНЫЕ (немедленно)

1. **[Priority 1]** Структура локализации

- Мигрировать русскую документацию в `docs/ru/` или изменить конфигурацию i18n
- **Время:** 2-3 часа
- **Важность:** Блокирует правильное отображение документации

2. **[Priority 2]** Обновить все версии компонентов

- Синхронизировать INDEX и OVERVIEW с compose.yml
- Особенно: Grafana (v11→v12), Fluent Bit (v3→v4)
- **Время:** 1 час
- **Важность:** Пользователи получают неверную информацию

3. **[Priority 3]** Исправить количество сервисов

- Обновить везде на 32 с указанием критичных, опциональных, монитор-only
- **Время:** 30 минут
- **Важность:** Противоречивая информация

4. **[Priority 4]** Создать Secrets Management Guide

- Пошаговое руководство по созданию всех 23 secrets
- Примеры содержимого
- Инструкции по permissions
- **Время:** 2 часа
- **Важность:** Без этого система не работает

### 🟠 ВЫСОКИЙ ПРИОРИТЕТ (неделя 1)

5. **[Priority 5]** Документировать недостающие сервисы

- Uptime Kuma: интеграция, примеры мониторов
- Docling: API reference, примеры использования
- MCP Server: инструменты, API endpoints
- postgres-exporter-proxy: назначение, troubleshooting
- **Время:** 4-5 часов
- **Важность:** Пользователи не знают о существовании/использовании этих
  сервисов

6. **[Priority 6]** Создать полный список env-файлов

- Все требуемые env переменные
- Значения по умолчанию
- Примеры конфигурации
- **Время:** 3 часа
- **Важность:** Пользователи не знают, какие файлы нужны

7. **[Priority 7]** API Documentation

- Webhook Receiver endpoints
- MCP Server tools
- Payload formats
- Примеры интеграции
- **Время:** 4 часа
- **Важность:** Критично для разработчиков

8. **[Priority 8]** Проверить существование скриптов

- Аудит всех ссылок на скрипты
- Удалить несуществующие или создать их
- **Время:** 1-2 часа
- **Важность:** Пользователи будут искать несуществующие скрипты

### [WARNING] СРЕДНИЙ ПРИОРИТЕТ (неделя 2-3)

9. **[Priority 9]** Документировать 4-tier logging strategy

- Объяснение каждого tier
- Рекомендации по изменению
- Troubleshooting
- **Время:** 2 часа

10. **[Priority 10]** GPU Optimization Guide

- Рекомендации для разных GPU моделей
- Memory management
- Performance tuning
- **Время:** 3 часа

11. **[Priority 11]** Update Strategy документация

- Watchtower selective updates
- Manual update procedures
- Testing updates
- **Время:** 2 часа

12. **[Priority 12]** Security Policy документация

- Root container risks
- Secrets management best practices
- Rotation procedures
- **Время:** 3 часа

### НИЗКИЙ ПРИОРИТЕТ (месяц 2)

13. Migration guides для major version updates
14. Performance benchmarking guide
15. Troubleshooting matrix для common issues
16. Architecture decision records (ADR)

---

## ЗАКЛЮЧЕНИЕ

### Статистика расхождений

| Категория                      | Количество | Статус            |
| ------------------------------ | ---------- | ----------------- |
| Версии компонентов (отстают)   | 8+         | КРИТИЧНЫЕ         |
| Недокументированные сервисы    | 6          | КРИТИЧНЫЕ         |
| Несоответствия портов          | 2+         | 🟠 ВАЖНЫЕ         |
| Отсутствующие env-файлы        | 15+        | КРИТИЧНЫЕ         |
| Отсутствующие API документации | 3          | 🟠 ВАЖНЫЕ         |
| Отсутствующие руководства      | 10+        | [WARNING] СРЕДНИЕ |

### Общий риск: ВЫСОКИЙ

Без синхронизации документации и кода пользователи будут сталкиваться с:

- Ошибками при развертывании (неверные версии, несуществующие сервисы)
- Неработающей конфигурацией (отсутствующие env-файлы, secrets)
- Невозможностью интеграции (отсутствующие API docs)
- Проблемами с мониторингом и логированием

### Рекомендуемый план действий

**Этап 1 (2-3 дня) - КРИТИЧНОЕ:**

1. Мигрировать локализацию в docs/ru/
2. Обновить версии компонентов
3. Создать Secrets Management Guide
4. Исправить количество сервисов

**Этап 2 (1 неделя) - ВАЖНОЕ:** 5. Документировать недостающие сервисы 6.
Создать env-файлы guide 7. Написать API documentation 8. Проверить скрипты

**Этап 3 (2 недели) - СРЕДНЕЕ:** 9. 4-tier logging strategy 10. GPU optimization
guide 11. Update strategy docs 12. Security policy docs

---

**Дата аудита:** 2025-11-30 **Версия проекта:** v0.6.3 **Проверены файлы:** 50+
документов, compose.yml (1364 строк), исходные коды сервисов **Статус:** ТРЕБУЕТ
НЕМЕДЛЕННОГО ВНИМАНИЯ
