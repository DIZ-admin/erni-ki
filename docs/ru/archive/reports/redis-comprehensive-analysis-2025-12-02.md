---
title: Комплексный анализ Redis в ERNI-KI
date: 2025-12-02
language: ru
doc_version: '2025.11'
translation_status: complete
description: Комплексный аудит Redis 7.0.15-alpine в ERNI-KI
tags:
  - redis
  - audit
  - performance
  - security
---

## Краткое резюме (Executive Summary)

Проведен комплексный анализ сервиса Redis в проекте ERNI-KI. Выявлено **5
критических** и **8 важных** проблем, требующих немедленного исправления.
Основные риски связаны с безопасностью (открытый порт на публичном интерфейсе) и
производительностью (критическая фрагментация памяти 5.55x).

**Уровень риска:** КРИТИЧЕСКИЙ **Рекомендуемые действия:** Немедленные

---

## 1. Текущее состояние Redis

### 1.1 Общая информация

```yaml
Версия: Redis 7.0.15 (alpine)
Uptime: ~16 часов (с 02.12.2025 08:52:05)
Режим работы: standalone (без репликации)
Персистентность: AOF (appendonly.aof)
Container: erni-ki-redis-1
Статус: healthy
```

### 1.2 Метрики производительности

```
Всего подключений: 675
Всего команд: 3885
Ops/sec (текущие): 0-1 (очень низкая нагрузка)
Keyspace hits: 2224
Keyspace misses: 341
Hit rate: 86.7% (хороший показатель)
Количество ключей: 147
```

### 1.3 Использование памяти

```
Used memory: 1.41 MB (0.07% от лимита 2GB)
RSS memory: 7.83 MB
Peak memory: 1.41 MB
Fragmentation ratio: 5.55x КРИТИЧНО!
Max memory: 2 GB
Memory policy: allkeys-lru
Active defrag: enabled
```

### 1.4 Подключения

```
Connected clients: 6
Blocked clients: 0
Max clients: 10000
Rejected connections: 0
```

---

## 2. Критические проблемы (CRITICAL)

### CRIT-1: Порт Redis открыт на всех интерфейсах

**Описание:** Redis порт 6379 доступен на всех сетевых интерфейсах (0.0.0.0),
что делает его доступным из внешней сети.

**Доказательство:**

```bash
$ netstat -tlnp | grep 6379
tcp 0 0 0.0.0.0:6379 0.0.0.0:* LISTEN
tcp6 0 0 :::6379 :::* LISTEN
```

**Конфигурация в compose.yml (строка 125-172):**

```yaml
redis:
  # НЕТ ограничения портов на localhost!
  # ОТСУТСТВУЕТ: ports: ["127.0.0.1:6379:6379"]
```

**Риск:** Высокий **Воздействие:** Redis может быть доступен из внешней сети,
что создает уязвимость для атак, даже при наличии пароля.

**Рекомендация:**

```yaml
# В compose.yml НЕ публиковать порт Redis вообще!
# Redis должен быть доступен только внутри Docker сети
redis:
  # Удалить или закомментировать expose/ports
  # expose:
  # - "6379"
```

**Приоритет:** КРИТИЧЕСКИЙ - исправить немедленно

---

### CRIT-2: Критическая фрагментация памяти (5.55x)

**Описание:** Redis использует в 5.55 раз больше физической памяти (RSS 7.83MB)
чем логически требуется (used 1.41MB). Нормальное значение: 1.0-1.5x.

**Метрики:**

```
used_memory_rss: 8212480 байт (7.83 MB)
used_memory: 1479952 байт (1.41 MB)
mem_fragmentation_ratio: 5.55x
```

**Причины:**

1. Частые операции записи/удаления малых объектов
2. Недостаточная эффективность active defragmentation
3. Низкие пороги для активации дефрагментации

**Воздействие:**

- Неэффективное использование памяти (5.55x overhead)
- Возможная деградация производительности при росте данных
- Риск достижения лимитов памяти раньше ожидаемого

**Конфигурация дефрагментации
([conf/redis/redis.conf:84-109](conf/redis/redis.conf)):**

```ini
activedefrag yes
active-defrag-ignore-bytes 1mb # Низкий порог
active-defrag-threshold-lower 5 # Слишком агрессивный
active-defrag-threshold-upper 50 # Хорошо
active-defrag-cycle-min 5 # Может быть увеличен
active-defrag-cycle-max 75 # Слишком высокий CPU usage
```

**Рекомендации:**

1. **Немедленно:** Перезапустить Redis для сброса фрагментации

```bash
docker compose restart redis
```

2. **Долгосрочно:** Оптимизировать параметры дефрагментации:

```ini
# Более сбалансированные настройки
active-defrag-threshold-lower 10 # Было: 5
active-defrag-threshold-upper 100 # Было: 50
active-defrag-cycle-min 1 # Было: 5
active-defrag-cycle-max 25 # Было: 75
active-defrag-ignore-bytes 10mb # Было: 1mb
```

3. **Мониторинг:** Настроить более жесткие алерты в
   [conf/prometheus/rules/redis-alerts.yml:68-96](conf/prometheus/rules/redis-alerts.yml):

```yaml
# Было: > 3.0 for 10m
# Стало:
- alert: RedisHighMemoryFragmentation
expr: redis_mem_fragmentation_ratio > 2.0
for: 5m # Сократить время
```

**Приоритет:** КРИТИЧЕСКИЙ

---

### CRIT-3: Хардкод пароля в конфигурации SearXNG

**Описание:** Пароль Redis жестко закодирован в файле конфигурации SearXNG в
открытом виде.

**Местоположение:** [conf/searxng/settings.yml:132](conf/searxng/settings.yml)

```yaml
valkey:
  # УЯЗВИМОСТЬ: Пароль в открытом виде!
  url: redis://:ErniKiRedisSecurePassword2024@redis:6379/0
```

**Риск:** Критический **Воздействие:**

- Пароль виден в системе контроля версий
- Доступ к Redis при компрометации файловой системы
- Нарушение best practices безопасности

**Существующее решение:** Есть entrypoint скрипт
[scripts/entrypoints/searxng.sh:20-33](scripts/entrypoints/searxng.sh), который
ДОЛЖЕН переопределять URL через Docker secrets:

```bash
configure_redis_url() {
 if password="$(read_secret "redis_password")"; then
 export SEARXNG_REDIS_URL="redis://:${password}@${host}:${port}/${db}"
 fi
}
```

**Проблема:** Хардкод пароль в settings.yml НЕ переопределяется переменной
окружения!

**Рекомендации:**

1. **Немедленно:** Удалить пароль из settings.yml:

```yaml
valkey:
# Использовать переменную окружения SEARXNG_REDIS_URL
url: '' # Пустое значение - будет установлено через env
```

2. **Проверить:** Убедиться, что SearXNG использует `SEARXNG_REDIS_URL` из
   окружения:

```bash
docker exec erni-ki-searxng-1 env | grep SEARXNG_REDIS_URL
```

3. **Сменить пароль:** После исправления сменить скомпрометированный пароль

```bash
# Сгенерировать новый пароль
openssl rand -base64 32 > secrets/redis_password.txt
# Перезапустить Redis и зависимые сервисы
docker compose restart redis searxng openwebui
```

**Приоритет:** КРИТИЧЕСКИЙ

---

### CRIT-4: Несогласованность использования БД Redis

**Описание:** SearXNG конфигурируется с использованием разных database ID в
разных местах.

**Конфликт:**

- **settings.yml:** `redis://:password@redis:6379/0` (database 0)
- **entrypoint.sh:** `local db="${SEARXNG_REDIS_DB:-1}"` (database 1)

**Код entrypoint
([scripts/entrypoints/searxng.sh:23](scripts/entrypoints/searxng.sh)):**

```bash
local db="${SEARXNG_REDIS_DB:-1}" # Default DB = 1
```

**Конфигурация settings.yml
([conf/searxng/settings.yml:132](conf/searxng/settings.yml)):**

```yaml
url: redis://:ErniKiRedisSecurePassword2024@redis:6379/0 # DB = 0
```

**Воздействие:**

- Непредсказуемое поведение при использовании разных баз
- Возможная потеря данных кэша
- Сложность отладки проблем

**Проверка текущего состояния:**

```bash
# DB 0: 147 ключей (из текущего анализа)
# DB 1: неизвестно
```

**Рекомендации:**

1. **Стандартизировать на DB 0:**

```bash
# В scripts/entrypoints/searxng.sh:23
local db="${SEARXNG_REDIS_DB:-0}" # Изменить с 1 на 0
```

2. **Или стандартизировать на DB 1:**

```yaml
# В conf/searxng/settings.yml:132 (после удаления пароля)
url: '' # Будет: redis://:password@redis:6379/1 из entrypoint
```

3. **Документировать выбор:**

```yaml
# Redis Database Allocation:
# DB 0: SearXNG cache and limiter
# DB 1: Reserved for future use (LiteLLM?)
# DB 2-15: Available
```

**Приоритет:** ВЫСОКИЙ

---

### CRIT-5: ACL слишком разрешительный

**Описание:** Default пользователь имеет полный доступ ко всем командам, ключам
и каналам.

**Текущая конфигурация ([conf/redis/users.acl:1](conf/redis/users.acl)):**

```acl
user default on >__LOAD_FROM_DOCKER_SECRET__ ~* &* +@all
```

**Разбор:**

- `~*` - доступ ко всем ключам (все namespace)
- `&*` - доступ ко всем pub/sub каналам
- `+@all` - все команды разрешены

**Риск:** При компрометации пароля атакующий получает полный доступ ко всем
данным и операциям Redis.

**Рекомендации:**

1. **Создать ограниченные ACL для каждого сервиса:**

```acl
# Default user - только для административных задач
user default on >__LOAD_FROM_DOCKER_SECRET__ ~* &* +@admin +@dangerous

# SearXNG user - только кэш и pub/sub
user searxng on >__LOAD_FROM_DOCKER_SECRET__ ~searxng:* &searxng:* +@read +@write +@pubsub -@dangerous

# OpenWebUI user - WebSocket и сессии
user openwebui on >__LOAD_FROM_DOCKER_SECRET__ ~openwebui:* &openwebui:* +@read +@write +@pubsub -@dangerous

# LiteLLM user - кэширование (если будет включено)
user litellm on >__LOAD_FROM_DOCKER_SECRET__ ~litellm:* &* +@read +@write -@dangerous

# Redis Exporter - только чтение метрик
user exporter on >__LOAD_FROM_DOCKER_SECRET__ ~* &* +@read +INFO +SLOWLOG +CLIENT -@write -@dangerous
```

2. **Обновить подключения сервисов для использования специфичных пользователей**

3. **Disabled опасные команды** уже правильно настроены в
   [conf/redis/redis.conf:28-33](conf/redis/redis.conf), но CONFIG всё ещё
   доступен (закомментирован)

**Приоритет:** ВЫСОКИЙ

---

## 3. Важные проблемы (HIGH)

### HIGH-1: LiteLLM не использует Redis для кэширования

**Описание:** Redis настройки закомментированы в конфигурации LiteLLM, что
означает отсутствие кэширования на уровне роутера.

**Код ([conf/litellm/config.yaml:38-42](conf/litellm/config.yaml)):**

```yaml
router_settings:
  # Redis settings for router are temporarily disabled due to incompatibility
  # redis_host: "redis"
  # redis_port: 6379
  # redis_password: "${REDIS_PASSWORD}"
  # redis_db: 1
```

**Также кэширование включено, но неясно какой backend:**

```yaml
litellm_settings:
  cache: true # Какой backend используется?
```

**Воздействие:**

- Отсутствие кэширования повторяющихся запросов к LLM
- Повышенная нагрузка на Ollama/внешние модели
- Увеличенное время ответа для дублирующихся промптов
- Повышенная стоимость API вызовов (для платных моделей)

**Рекомендации:**

1. **Выяснить причину несовместимости** и исправить
2. **Включить Redis для LiteLLM:**

```yaml
router_settings:
redis_host: 'redis'
redis_port: 6379
redis_password: '${REDIS_PASSWORD}'
redis_db: 2 # Отдельная БД для LiteLLM
```

3. **Настроить TTL кэша:**

```yaml
litellm_settings:
cache: true
cache_params:
ttl: 3600 # 1 час
type: 'redis'
```

**Приоритет:** ВЫСОКИЙ (влияет на производительность)

---

### HIGH-2: Отсутствие мониторинга медленных команд в реальном времени

**Описание:** Хотя slowlog настроен, нет автоматического мониторинга и алертинга
на медленные команды.

**Текущая конфигурация ([conf/redis/redis.conf:58-59](conf/redis/redis.conf)):**

```ini
slowlog-log-slower-than 10000 # 10ms
slowlog-max-len 128
```

**Алерт существует
([conf/prometheus/rules/redis-alerts.yml:193-204](conf/prometheus/rules/redis-alerts.yml)):**

```yaml
- alert: RedisSlowlogGrowing
 expr: increase(redis_slowlog_length[5m]) > 10
 for: 5m
```

**Проблема:** Алерт срабатывает только при росте slowlog, но не показывает
конкретные медленные команды.

**Рекомендации:**

1. **Добавить экспорт slowlog в Prometheus:**

- Создать custom exporter для анализа SLOWLOG GET
- Экспортировать топ медленных команд как метрики

2. **Создать дашборд в Grafana** для визуализации slowlog

3. **Снизить порог для production:**

```ini
slowlog-log-slower-than 5000 # 5ms вместо 10ms
```

**Приоритет:** ВЫСОКИЙ

---

### HIGH-3: Недостаточная документация использования Redis

**Описание:** Нет централизованной документации о том, как Redis используется в
проекте.

**Отсутствует:**

- Mapping: какой сервис использует какую БД
- Naming conventions для ключей
- TTL политики для разных типов данных
- Процедуры восстановления при проблемах

**Рекомендации:** Создать документ `docs/en/architecture/redis-usage.md`:

```markdown
# Redis Usage in ERNI-KI

## Database Allocation

- DB 0: SearXNG (cache, limiter, bot detection)
- DB 1: Reserved
- DB 2: LiteLLM (model caching)
- DB 3-15: Available for future use

## Naming Conventions

- SearXNG: `searxng:*`
- OpenWebUI: `openwebui:session:*`, `openwebui:websocket:*`
- LiteLLM: `litellm:cache:*`

## TTL Policies

- Session data: 24 hours
- Cache data: 1 hour
- Rate limit counters: 5 minutes

## Monitoring

- Grafana: http://grafana.erni-ki.local/d/redis
- Prometheus: http://prometheus.erni-ki.local:9091
- Redis Exporter: http://localhost:9121/metrics
```

**Приоритет:** СРЕДНИЙ

---

### HIGH-4: Отсутствие резервного копирования Redis данных

**Описание:** Хотя AOF включен, RDB snapshots отключены, и нет интеграции с
Backrest.

**Текущая конфигурация ([conf/redis/redis.conf:19-20](conf/redis/redis.conf)):**

```ini
# === СНАПШОТЫ (отключены для производительности) ===
save "" # Все RDB snapshots отключены
```

**AOF настройки ([conf/redis/redis.conf:44-52](conf/redis/redis.conf)):**

```ini
appendonly yes
appendfilename "appendonly.aof"
appendfsync everysec
```

**Проблемы:**

1. AOF файл может быть поврежден при сбое диска
2. Нет периодических бэкапов AOF в Backrest
3. Восстановление возможно только из последнего AOF (нет точек восстановления)

**Рекомендации:**

1. **Включить периодические RDB snapshots:**

```ini
# Создавать snapshot при изменении хотя бы 1 ключа за 1 час
save 3600 1
save 7200 1 # Дополнительный snapshot каждые 2 часа
```

2. **Добавить Redis данные в Backrest:**

```yaml
# В compose.yml для backrest:
volumes:
  - ./data/redis:/backup-sources/redis:ro
```

3. **Настроить автоматическое сохранение AOF в Backrest:**

```bash
# Cron job для ротации AOF в backup
0 */6 * * * cp -a /app/data/redis/appendonly.aof* /backup/redis-aof-$(date +\%Y\%m\%d-\%H\%M\%S)/
```

**Приоритет:** ВЫСОКИЙ

---

### HIGH-5: Низкий maxmemory при включенном AOF

**Описание:** При использовании AOF, Redis может потреблять до 2x памяти во
время rewrite операций.

**Текущая конфигурация:**

```ini
maxmemory 2gb
appendonly yes
auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size 64mb
```

**Риск:** Во время AOF rewrite Redis может использовать до 4GB RAM (2x
maxmemory), что превышает container limit 1GB в compose.yml.

**Container limits ([compose.yml:170-171](compose.yml)):**

```yaml
redis:
  mem_limit: 1g # ПРОБЛЕМА: меньше чем maxmemory!
  cpus: '1.0'
```

**Рекомендации:**

1. **Увеличить container memory limit:**

```yaml
redis:
mem_limit: 4g # 2x maxmemory для AOF rewrite
mem_reservation: 2g # Гарантированная память
```

2. **Или снизить maxmemory Redis:**

```ini
maxmemory 512mb # Половина container limit
```

3. **Мониторинг AOF rewrite:**

```yaml
- alert: RedisAOFRewriteMemorySpike
expr:
redis_aof_rewrite_in_progress == 1 and
container_memory_usage_bytes{name="redis"} > 800000000
```

**Приоритет:** КРИТИЧЕСКИЙ (может вызвать OOM kill)

---

### HIGH-6: Отсутствие rate limiting на Redis операции

**Описание:** Нет защиты от flood атак на Redis через клиентские приложения.

**Риски:**

- DoS атака через SearXNG (flood поисковых запросов)
- Переполнение памяти через массовую запись ключей
- Исчерпание соединений

**Рекомендации:**

1. **Настроить client-output-buffer-limit более строго:**

```ini
# Текущее: unlimited для normal клиентов
client-output-buffer-limit normal 0 0 0

# Рекомендуется:
client-output-buffer-limit normal 10mb 5mb 60
```

2. **Добавить timeout для idle соединений:**

```ini
timeout 300 # Текущее: 0 (отключено)
```

3. **Мониторинг аномальной активности:**

```yaml
- alert: RedisUnusualOperationsRate
expr: rate(redis_commands_processed_total[1m]) > 1000
for: 2m
```

**Приоритет:** СРЕДНИЙ

---

### HIGH-7: CONFIG команда всё ещё доступна

**Описание:** Хотя есть закомментированное переименование CONFIG, команда всё
ещё доступна.

**Код ([conf/redis/redis.conf:32-33](conf/redis/redis.conf)):**

```ini
# CONFIG временно разрешен для мониторинга (2025-10-02)
# rename-command CONFIG "CONFIG_b835c3f8a5d9e7f2a1b4c6d8e9f0a2b3"
```

**Проверка:**

```bash
$ docker exec erni-ki-redis-1 sh -c 'redis-cli --pass "$(cat /run/secrets/redis_password)" CONFIG GET rename-command'
# Вернуло пустой результат - команды НЕ переименованы!
```

**Риск:** CONFIG позволяет изменить настройки Redis в runtime, включая
безопасность.

**Рекомендации:**

1. **Переименовать CONFIG для admin использования:**

```ini
rename-command CONFIG "CONFIG_b835c3f8a5d9e7f2a1b4c6d8e9f0a2b3"
```

2. **Обновить redis-exporter для использования переименованной команды:**

- Использовать ACL read-only user вместо CONFIG

3. **Документировать renamed commands:**

```bash
# В docs/en/operations/core/operations-handbook.md
## Redis Administrative Commands
- CONFIG: renamed to CONFIG_b835c3f8a5d9e7f2a1b4c6d8e9f0a2b3
```

**Приоритет:** СРЕДНИЙ-ВЫСОКИЙ

---

### HIGH-8: Отсутствие TLS для Redis подключений

**Описание:** Все подключения к Redis происходят в открытом виде (plain text).

**Текущая конфигурация:**

- Нет TLS сертификатов для Redis
- Нет настроек TLS в redis.conf
- Клиенты подключаются без шифрования

**Риск:** В контексте Docker сети - низкий, но при сетевой компрометации
возможен перехват данных.

**Рекомендации:**

**Низкий приоритет для текущей конфигурации** (Redis в изолированной Docker
сети), но для production:

1. **Включить TLS в Redis:**

```ini
# redis.conf
tls-port 6380
port 0 # Отключить non-TLS порт
tls-cert-file /etc/redis/certs/redis.crt
tls-key-file /etc/redis/certs/redis.key
tls-ca-cert-file /etc/redis/certs/ca.crt
```

2. **Обновить клиентов для TLS:**

```yaml
# SearXNG
url: rediss://:password@redis:6380/0
```

**Приоритет:** ℹ НИЗКИЙ (для текущей конфигурации)

---

## 4. Средние проблемы (MEDIUM)

### MED-1: Неоптимальные настройки hash/list compression

**Текущие настройки ([conf/redis/redis.conf:65-74](conf/redis/redis.conf)):**

```ini
hash-max-ziplist-entries 512
hash-max-ziplist-value 64
list-max-ziplist-size -2
zset-max-ziplist-entries 128
zset-max-ziplist-value 64
```

**Рекомендации для SearXNG use case (много маленьких ключей):**

```ini
# Увеличить для лучшей компрессии кэша
hash-max-ziplist-entries 1024 # Было: 512
hash-max-ziplist-value 128 # Было: 64
list-max-ziplist-size -3 # Больше компрессии
```

---

### MED-2: Hz слишком низкий для active defrag

**Текущая настройка ([conf/redis/redis.conf:79-80](conf/redis/redis.conf)):**

```ini
hz 10
dynamic-hz yes
```

**Рекомендации:**

```ini
hz 50 # Увеличить для более частых фоновых задач (defrag, expire)
```

**Воздействие на производительность:** Минимальное при текущей нагрузке

---

### MED-3: Отсутствие мониторинга ключей по типу

**Описание:** Неясно какие типы данных занимают память в Redis.

**Рекомендации:** Добавить периодический анализ:

```bash
# Cron job для анализа типов ключей
*/30 * * * * docker exec erni-ki-redis-1 redis-cli \
 --pass "$(cat /run/secrets/redis_password)" \
 --scan --pattern '*' | \
 xargs -I{} docker exec erni-ki-redis-1 redis-cli \
 --pass "$(cat /run/secrets/redis_password)" \
 TYPE {} | sort | uniq -c > /var/log/redis-key-types.log
```

---

## 5. Информационные замечания (INFO)

### ℹ INFO-1: Низкая нагрузка на Redis

**Наблюдение:**

```
Ops/sec: 0-1
Keys: 147
Connections: 6
Memory: 1.41 MB / 2 GB (0.07%)
```

**Комментарий:** Redis сильно недогружен. Это хорошо для стабильности, но
означает, что многие проблемы могут проявиться только при росте нагрузки.

---

### ℹ INFO-2: AOF файл не слишком большой

**Наблюдение:** Алерт RedisAOFFileTooLarge
([conf/prometheus/rules/redis-alerts.yml:224-235](conf/prometheus/rules/redis-alerts.yml))
проверяет размер > 100MB.

При текущей нагрузке и данных (1.41MB) это не проблема.

---

### ℹ INFO-3: Хороший hit rate (86.7%)

**Метрики:**

```
Keyspace hits: 2224
Keyspace misses: 341
Hit rate: 86.7%
```

**Комментарий:** Отличный показатель для кэша. Целевое значение: >80%, текущее:
86.7%

---

## 6. Рекомендации по приоритетам

### Немедленно (в течение 24 часов)

1. **CRIT-1:** Убрать публикацию порта Redis (удалить из compose.yml)
2. **CRIT-2:** Перезапустить Redis для сброса фрагментации памяти
3. **CRIT-3:** Удалить хардкод пароля из conf/searxng/settings.yml
4. **HIGH-5:** Увеличить container memory limit до 4GB или снизить maxmemory до
   512MB

### В течение недели

1. **CRIT-4:** Стандартизировать использование Redis DB (выбрать 0 или 1)
2. **CRIT-5:** Настроить ACL для разных сервисов
3. **HIGH-1:** Включить Redis кэширование в LiteLLM
4. **HIGH-4:** Настроить резервное копирование Redis данных в Backrest
5. **HIGH-7:** Переименовать CONFIG команду

### В течение месяца

1. **HIGH-2:** Настроить детальный мониторинг slowlog
2. **HIGH-3:** Создать документацию по использованию Redis
3. **HIGH-6:** Настроить rate limiting на Redis операции
4. **MED-1:** Оптимизировать настройки compression
5. **MED-2:** Увеличить hz для лучшей дефрагментации

### Backlog (низкий приоритет)

1. **HIGH-8:** Настроить TLS для Redis (если планируется вынос из Docker сети)
2. **MED-3:** Настроить мониторинг типов ключей

---

## 7. План действий

### Этап 1: Критические исправления безопасности (день 1)

```bash
# 1. Остановить систему
docker compose down

# 2. Сменить пароль Redis
openssl rand -base64 32 > secrets/redis_password.txt

# 3. Редактировать compose.yml - убрать публикацию порта Redis
# Удалить или закомментировать строки с expose/ports для redis

# 4. Редактировать conf/searxng/settings.yml - убрать хардкод пароль
# Строка 132: url: ""

# 5. Редактировать scripts/entrypoints/searxng.sh - стандартизировать DB
# Строка 23: local db="${SEARXNG_REDIS_DB:-0}"

# 6. Увеличить memory limit для Redis в compose.yml
# mem_limit: 4g
# mem_reservation: 2g

# 7. Запустить систему
docker compose up -d

# 8. Проверить подключения
docker logs erni-ki-redis-1 --tail 50
docker logs erni-ki-searxng-1 --tail 50
docker exec erni-ki-redis-1 redis-cli --pass "$(cat secrets/redis_password.txt)" PING
```

### Этап 2: Оптимизация производительности (неделя 1)

```bash
# 1. Оптимизировать conf/redis/redis.conf
# - Настроить active defrag (см. CRIT-2 рекомендации)
# - Увеличить hz до 50
# - Оптимизировать compression settings

# 2. Перезапустить Redis
docker compose restart redis

# 3. Мониторить фрагментацию
watch -n 60 'docker exec erni-ki-redis-1 redis-cli --pass "$(cat secrets/redis_password.txt)" INFO memory | grep fragmentation'
```

### Этап 3: ACL и безопасность (неделя 1-2)

```bash
# 1. Создать новый conf/redis/users.acl с ограниченными правами
# (см. CRIT-5 рекомендации)

# 2. Обновить подключения сервисов для использования специфичных пользователей

# 3. Переименовать опасные команды в redis.conf
# rename-command CONFIG "CONFIG_b835c3f8a5d9e7f2a1b4c6d8e9f0a2b3"

# 4. Перезапустить Redis
docker compose restart redis

# 5. Протестировать подключения всех сервисов
docker compose ps
docker logs erni-ki-searxng-1
docker logs erni-ki-openwebui-1
```

### Этап 4: LiteLLM Redis интеграция (неделя 2-3)

```bash
# 1. Выяснить причину несовместимости LiteLLM с Redis

# 2. Включить Redis в conf/litellm/config.yaml

# 3. Перезапустить LiteLLM
docker compose restart litellm

# 4. Проверить кэширование
docker exec erni-ki-redis-1 redis-cli --pass "$(cat secrets/redis_password.txt)" KEYS "litellm:*"
```

### Этап 5: Резервное копирование (неделя 3-4)

```bash
# 1. Включить RDB snapshots в redis.conf

# 2. Добавить Redis данные в Backrest volumes

# 3. Настроить периодическое копирование AOF

# 4. Протестировать восстановление из бэкапа
```

---

## 8. Мониторинг и алертинг

### Текущие алерты

Отличная система алертов уже настроена в
[conf/prometheus/rules/redis-alerts.yml](conf/prometheus/rules/redis-alerts.yml):

**Хорошо покрыто:**

- Availability (RedisDown, RedisRestarted)
- Memory (High/Critical usage, Fragmentation)
- Connections (TooMany, Rejected, Blocked)
- Performance (Low hit rate, High latency, Slowlog)
- Persistence (AOF rewrite, AOF size)
- Replication (Master link, Lag)
- Exporter health

### Рекомендации по улучшению алертов

1. **Обновить пороги фрагментации:**

```yaml
# Текущий: > 3.0 for 10m
# Рекомендуется: > 2.0 for 5m
```

2. **Добавить алерт на OOM риск:**

```yaml
- alert: RedisMemoryNearContainerLimit
expr:
container_memory_usage_bytes{name="redis"} /
container_spec_memory_limit_bytes{name="redis"} > 0.85
for: 5m
labels:
severity: critical
annotations:
summary: 'Redis approaching container memory limit'
action: 'Increase container mem_limit or reduce maxmemory'
```

3. **Добавить алерт на несогласованность конфигурации:**

```yaml
- alert: RedisConfigDrift
expr: redis_config_maxmemory != 2147483648 # Expected 2GB
for: 5m
labels:
severity: warning
annotations:
summary: 'Redis configuration drift detected'
```

---

## 9. Выводы

### Общая оценка

Redis в ERNI-KI настроен **удовлетворительно**, но имеет **критические проблемы
безопасности** и **проблемы производительности**, требующие немедленного
внимания.

**Положительные аспекты:**

- Включена персистентность (AOF)
- Настроена активная дефрагментация
- Отличная система мониторинга и алертинга
- Хороший cache hit rate (86.7%)
- Отключены опасные команды (FLUSHDB, FLUSHALL, DEBUG)
- Используются Docker secrets для паролей
- Настроены lazy-free операции

**Критические недостатки:**

- Порт Redis открыт на публичном интерфейсе
- Критическая фрагментация памяти (5.55x)
- Хардкод пароля в конфигурации SearXNG
- Несогласованность использования БД Redis
- Container memory limit меньше maxmemory (риск OOM)
- ACL слишком разрешительный

### Риск-профиль

| Категория          | Оценка                      | Комментарий                            |
| ------------------ | --------------------------- | -------------------------------------- |
| Безопасность       | КРИТИЧЕСКИЙ                 | Открытый порт + хардкод пароля         |
| Производительность | 🟠 СРЕДНИЙ                  | Фрагментация 5.55x, но низкая нагрузка |
| Надежность         | [WARNING] УДОВЛЕТВОРИТЕЛЬНО | AOF включен, но нет RDB бэкапов        |
| Мониторинг         | [OK] ОТЛИЧНО                | Полная система алертов                 |
| Документация       | 🟠 НЕДОСТАТОЧНО             | Нет документации по использованию      |

### Приоритетные действия (Top 5)

1. **Немедленно:** Убрать публикацию порта Redis на публичном интерфейсе
2. **Немедленно:** Исправить container memory limit (4GB) или maxmemory (512MB)
3. **День 1:** Удалить хардкод пароля из SearXNG конфигурации и сменить пароль
4. **Неделя 1:** Перезапустить Redis для сброса фрагментации и настроить
   дефрагментацию
5. **Неделя 1:** Настроить ACL для разных сервисов

---

## 10. Дополнительные ресурсы

### Полезные команды для диагностики

```bash
# Статистика памяти
docker exec erni-ki-redis-1 redis-cli --pass "$(cat secrets/redis_password.txt)" INFO memory

# Статистика производительности
docker exec erni-ki-redis-1 redis-cli --pass "$(cat secrets/redis_password.txt)" INFO stats

# Список медленных команд
docker exec erni-ki-redis-1 redis-cli --pass "$(cat secrets/redis_password.txt)" SLOWLOG GET 10

# Список подключенных клиентов
docker exec erni-ki-redis-1 redis-cli --pass "$(cat secrets/redis_password.txt)" CLIENT LIST

# Анализ типов ключей
docker exec erni-ki-redis-1 redis-cli --pass "$(cat secrets/redis_password.txt)" --scan --pattern '*' | head -20

# Проверка размера базы данных
docker exec erni-ki-redis-1 redis-cli --pass "$(cat secrets/redis_password.txt)" DBSIZE

# Информация о персистентности
docker exec erni-ki-redis-1 redis-cli --pass "$(cat secrets/redis_password.txt)" INFO persistence

# Проверка фрагментации в реальном времени
watch -n 10 'docker exec erni-ki-redis-1 redis-cli --pass "$(cat secrets/redis_password.txt)" INFO memory | grep -E "(used_memory_rss|used_memory:|mem_fragmentation)"'
```

### Ссылки на конфигурационные файлы

- [compose.yml:125-172](compose.yml) - Redis service configuration
- [conf/redis/redis.conf](conf/redis/redis.conf) - Redis main configuration
- [conf/redis/users.acl](conf/redis/users.acl) - Redis ACL configuration
- [conf/searxng/settings.yml:126-132](conf/searxng/settings.yml) - SearXNG Redis
  configuration
- [scripts/entrypoints/searxng.sh](scripts/entrypoints/searxng.sh) - SearXNG
  entrypoint with Redis setup
- [conf/litellm/config.yaml:38-42](conf/litellm/config.yaml) - LiteLLM Redis
  configuration
- [conf/prometheus/rules/redis-alerts.yml](conf/prometheus/rules/redis-alerts.yml) -
  Redis monitoring alerts

### Метрики Redis в Prometheus

```promql
# Доступность
redis_up

# Использование памяти
redis_memory_used_bytes / redis_memory_max_bytes

# Фрагментация
redis_mem_fragmentation_ratio

# Hit rate
rate(redis_keyspace_hits_total[5m]) / (rate(redis_keyspace_hits_total[5m]) + rate(redis_keyspace_misses_total[5m]))

# Latency
redis_commands_duration_seconds_total / redis_commands_processed_total

# Количество ключей
redis_db_keys{db="db0"}
```

---

**Конец отчета**

---

## Приложение A: Чеклист для быстрой проверки

```markdown
### Безопасность

- [ ] Порт Redis НЕ опубликован на 0.0.0.0
- [ ] Пароль НЕ хардкоден в конфигурационных файлах
- [ ] ACL настроены для каждого сервиса
- [ ] Опасные команды отключены или переименованы
- [ ] Используются Docker secrets

### Производительность

- [ ] Фрагментация < 2.0x
- [ ] Cache hit rate > 80%
- [ ] Container memory >= 2x maxmemory
- [ ] Active defragmentation включена
- [ ] Hz настроен оптимально

### Надежность

- [ ] AOF включен
- [ ] RDB snapshots настроены
- [ ] Данные реплицируются в Backrest
- [ ] Мониторинг и алерты работают
- [ ] Процедуры восстановления документированы

### Мониторинг

- [ ] Redis exporter работает
- [ ] Алерты настроены в Prometheus
- [ ] Дашборды созданы в Grafana
- [ ] Slowlog мониторится
- [ ] Логи централизованы

### Документация

- [ ] Использование Redis документировано
- [ ] Naming conventions определены
- [ ] Database allocation документирована
- [ ] Процедуры восстановления описаны
- [ ] Runbook создан для операторов
```

---

**Подготовлено:** Claude Code **Дата:** 2 декабря 2025 **Версия отчета:** 1.0
**Следующая проверка:** После исправления критических проблем
