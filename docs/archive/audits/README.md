# Archives — Audits

Canonical audit reports live in `docs/ru/archive/audits/` with localized copies
under `docs/en/` and `docs/de/`. This directory exists for tooling and must
remain with a README even when empty. Keep new audit material in the localized
folders; do not store content here.
