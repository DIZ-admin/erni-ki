# Archives — Incidents

Incident records live in `docs/ru/archive/incidents/` with translations in `docs/en/` and `docs/de/`. This placeholder keeps archive metadata consistent; store incident content in the localized folders, not here.
