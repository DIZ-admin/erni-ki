--- a/open_webui/config.py
+++ b/open_webui/config.py
@@ -2356,6 +2356,21 @@
     "DOCLING_SERVER_URL",
     "rag.docling_server_url",
     os.getenv("DOCLING_SERVER_URL", "http://docling:5001"),
+)
+DOCLING_USE_ASYNC = PersistentConfig(
+    "DOCLING_USE_ASYNC",
+    "rag.docling_use_async",
+    os.getenv("DOCLING_USE_ASYNC", "false").lower() == "true",
+)
+DOCLING_POLL_INTERVAL = PersistentConfig(
+    "DOCLING_POLL_INTERVAL",
+    "rag.docling_poll_interval",
+    float(os.getenv("DOCLING_POLL_INTERVAL", "3")),
+)
+DOCLING_MAX_POLL_ATTEMPTS = PersistentConfig(
+    "DOCLING_MAX_POLL_ATTEMPTS",
+    "rag.docling_max_poll_attempts",
+    int(os.getenv("DOCLING_MAX_POLL_ATTEMPTS", "600")),
 )
 
 docling_params = os.getenv("DOCLING_PARAMS", "")
