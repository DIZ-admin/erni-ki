coverage:
  status:
    project:
      default:
        target: 0.8
        threshold: 0.02
    patch:
      default:
        target: 0.8
        threshold: 0.02

comment:
  layout: "reach, diff, flags, files"
  behavior: default
