# JWT Authentication Service Configuration
AUTH_PORT=9090
# Secrets are injected from Docker secrets:
# WEBUI_SECRET_KEY is read from openwebui_secret_key by entrypoint
AUTH_JWT_SECRET=
AUTH_ALGORITHM=HS256
AUTH_TOKEN_EXPIRY=3600
