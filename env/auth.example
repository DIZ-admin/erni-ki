# Auth Service Configuration for ERNI-KI
# Authentication and authorization service settings

# === Mode ===
# Gin framework mode (release for prod, debug for dev)
GIN_MODE=release

# === JWT ===
# JWT secret key for signing tokens
# ⚠️ MUST change before production; generate via: openssl rand -hex 32
JWT_SECRET=CHANGE_THIS_JWT_SECRET_BEFORE_PRODUCTION

# === Database ===
# PostgreSQL connection URL (pulled from db.env)
# DATABASE_URL=postgresql://postgres:password@db:5432/openwebui
