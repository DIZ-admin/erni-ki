# SearXNG Configuration for ERNI-KI

# === Basics ===
SEARXNG_HOST=0.0.0.0:8080
SEARXNG_PORT=8080
SEARXNG_BIND_ADDRESS=0.0.0.0

# === Redis ===
SEARXNG_REDIS_URL=redis://redis:6379/1

# === Security ===
# IMPORTANT: Generate a unique secret key via: openssl rand -hex 32
SEARXNG_SECRET=CHANGE_BEFORE_GOING_LIVE_GENERATE_WITH_OPENSSL_RAND_HEX_32

# === URL ===
# Base URL (override in production)
SEARXNG_BASE_URL=https://<domain-name>/searxng

# === Safety features ===
# Enable rate limiting
SEARXNG_LIMITER=true

# Enable image proxy for safety
SEARXNG_IMAGE_PROXY=true

# Disable public instance mode
SEARXNG_PUBLIC_INSTANCE=false

# === Misc ===
# Debug mode (dev only)
SEARXNG_DEBUG=false

# HTTP protocol version (1.0 or 1.1)
SEARXNG_HTTP_PROTOCOL_VERSION=1.1

# === Search engine notes ===
# DuckDuckGo disabled in conf/searxng/settings.yml due to CAPTCHA
# Active engines: Startpage, Brave, Bing
# Typical performance: <3 seconds, 60+ results
