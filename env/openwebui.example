# OpenWebUI Configuration
OPENWEBUI_VERSION=v0.6.40
WEBUI_AUTH=true
# Secrets are injected from Docker secrets via entrypoint:
# - WEBUI_SECRET_KEY comes from openwebui_secret_key
# - DATABASE_URL is built automatically from postgres_password
WEBUI_JWT_SECRET_KEY=
DATABASE_URL=
