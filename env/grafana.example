# Grafana Configuration for ERNI-KI
# Metrics visualization and dashboards
# Updated: 2025-09-19 | Status: Production Ready (18 dashboards)
# Monitoring stack: optimized with fixed Prometheus queries

# === Basics ===
# Install mode (production for stability)
GF_INSTALL_PLUGINS=
# Disable plugin update checks
GF_INSTALL_PLUGINS_CHECK_FOR_UPDATES=false

# === Security ===
# Secret key for signing cookies (unique)
GF_SECURITY_SECRET_KEY=your-grafana-secret-key-here
# Admin user
GF_SECURITY_ADMIN_USER=admin
# Admin password (set a strong one)
GF_SECURITY_ADMIN_PASSWORD=your-admin-password-here
# Disable new user signups
GF_USERS_ALLOW_SIGN_UP=false
# Disable anonymous access
GF_AUTH_ANONYMOUS_ENABLED=false

# === Server ===
# Domain
GF_SERVER_DOMAIN=grafana.your-domain.com
# Root URL
GF_SERVER_ROOT_URL=http://grafana.your-domain.com:3000
# HTTP port
GF_SERVER_HTTP_PORT=3000
# Protocol
GF_SERVER_PROTOCOL=http

# === Database ===
# DB type (sqlite3 for simplicity)
GF_DATABASE_TYPE=sqlite3
# DB file path
GF_DATABASE_PATH=/var/lib/grafana/grafana.db

# === Logging ===
# Log level
GF_LOG_LEVEL=info
# Log mode
GF_LOG_MODE=console,file
# Log file path
GF_LOG_FILE_PATH=/var/log/grafana/grafana.log

# === Analytics ===
# Disable telemetry
GF_ANALYTICS_REPORTING_ENABLED=false
# Disable update checks
GF_ANALYTICS_CHECK_FOR_UPDATES=false

# === Plugins ===
# Allow unsigned plugins
GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS=
# Plugins path
GF_PATHS_PLUGINS=/var/lib/grafana/plugins

# === Dashboards (18, fully functional) ===
# Provisioning path
GF_PATHS_PROVISIONING=/etc/grafana/provisioning
# Default home dashboard
GF_DASHBOARDS_DEFAULT_HOME_DASHBOARD_PATH=/var/lib/grafana/dashboards/home.json

# === Optimized monitoring notes ===
# 18 Grafana dashboards (System Overview, AI Services, Infrastructure, Monitoring Stack, Security & Performance)
# Prometheus queries use fallbacks; target: <3s load times, optimized for AI infra

# === IMPORTANT BEFORE PRODUCTION ===
# ⚠️ Replace your-grafana-secret-key-here with a unique secret
# ⚠️ Replace your-admin-password-here with a strong admin password
# ⚠️ Set GF_SERVER_DOMAIN to your domain
# ⚠️ Verify all 18 dashboards load after startup
# ⚠️ Confirm Prometheus data source works
